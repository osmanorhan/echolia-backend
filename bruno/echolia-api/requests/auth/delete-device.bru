meta {
  name: Delete Device
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/auth/device/{{targetDeviceId}}
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{accessToken}}
}
